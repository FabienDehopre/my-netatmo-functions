{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": ["npm run lint:check", "npm run format:check", "npm run build"],
    "source": "functions"
  },
  "storage": {
    "rules": "storage.rules"
  }
}
