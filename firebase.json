{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": ["npm run lint", "npm run build"]
  },
  "storage": {
    "rules": "storage.rules"
  }
}
